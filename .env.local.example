# Server-side only (do NOT prefix with NEXT_PUBLIC_)
# Used by `POST /api/contact` to subscribe via MailerLite.
# Get your API key from: https://dashboard.mailerlite.com/integrations/api
MAILERLITE_API_KEY=
MAILERLITE_GROUP_ID=
